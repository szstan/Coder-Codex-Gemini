# Playwright MCP Server 使用指南

> **最后更新**: 2026-01-20
> **适用版本**: CCG v1.1.0+

---

## 📖 目录

1. [简介](#简介)
2. [安装和配置](#安装和配置)
3. [可用工具](#可用工具)
4. [基础使用](#基础使用)
5. [与 CCG 集成](#与-ccg-集成)
6. [最佳实践](#最佳实践)
7. [常见问题](#常见问题)

---

## 简介

### 什么是 Playwright MCP Server？

Playwright MCP Server 是一个 Model Context Protocol (MCP) 服务器，它将 Playwright 的浏览器自动化能力暴露为 MCP 工具，让 Claude 可以直接控制浏览器进行：
- Web 应用测试
- UI 交互自动化
- 截图和页面分析
- E2E 测试生成

### 核心优势

| 优势 | 说明 |
|------|------|
| **无需编写代码** | Claude 直接调用工具，无需手写 Playwright 脚本 |
| **实时交互** | 可以在对话中实时操作浏览器 |
| **自动化测试生成** | 记录操作并生成测试代码 |
| **跨浏览器支持** | 支持 Chromium、Firefox、WebKit |
| **与 CCG 集成** | 可以让 Coder 生成测试，Codex 审核 |

---

## 安装和配置

### 自动安装（推荐）

如果你已经运行过 CCG 的安装脚本，Playwright MCP Server 已经自动安装：

**Windows**:
```powershell
.\setup.ps1
```

**Unix/macOS**:
```bash
./setup.sh
```

### 手动安装

如果需要手动安装或重新安装：

**Windows (PowerShell)**:
```powershell
claude mcp add playwright -s user --transport stdio -e SYSTEMROOT=C:\Windows -- cmd /c npx -y @executeautomation/playwright-mcp-server
```

**Unix/macOS**:
```bash
claude mcp add playwright -s user --transport stdio -- npx -y @executeautomation/playwright-mcp-server
```

### 验证安装

检查 Playwright MCP Server 是否已注册：

```bash
claude mcp list
```

你应该看到 `playwright` 在列表中。

---

## 可用工具

Playwright MCP Server 提供了以下工具（通过 `mcp__playwright__` 前缀调用）：

### 浏览器控制

| 工具 | 功能 | 示例 |
|------|------|------|
| `playwright_navigate` | 导航到 URL | 打开网页 |
| `playwright_close` | 关闭浏览器 | 释放资源 |
| `playwright_go_back` | 后退 | 浏览器历史后退 |
| `playwright_go_forward` | 前进 | 浏览器历史前进 |
| `playwright_resize` | 调整视口大小 | 模拟不同设备 |

### 页面交互

| 工具 | 功能 | 示例 |
|------|------|------|
| `playwright_click` | 点击元素 | 点击按钮 |
| `playwright_fill` | 填充输入框 | 输入文本 |
| `playwright_select` | 选择下拉选项 | 选择选项 |
| `playwright_hover` | 悬停元素 | 触发悬停效果 |
| `playwright_press_key` | 按键 | 按 Enter 键 |
| `playwright_drag` | 拖拽元素 | 拖拽排序 |
| `playwright_upload_file` | 上传文件 | 文件上传 |

### iframe 操作

| 工具 | 功能 | 示例 |
|------|------|------|
| `playwright_iframe_click` | 点击 iframe 内元素 | 点击嵌入内容 |
| `playwright_iframe_fill` | 填充 iframe 内输入框 | 填充嵌入表单 |

### 页面分析

| 工具 | 功能 | 示例 |
|------|------|------|
| `playwright_screenshot` | 截图 | 保存页面截图 |
| `playwright_get_visible_text` | 获取可见文本 | 提取页面内容 |
| `playwright_get_visible_html` | 获取 HTML | 分析页面结构 |
| `playwright_console_logs` | 获取控制台日志 | 调试 JS 错误 |

### JavaScript 执行

| 工具 | 功能 | 示例 |
|------|------|------|
| `playwright_evaluate` | 执行 JS 代码 | 操作 DOM |

### HTTP 请求

| 工具 | 功能 | 示例 |
|------|------|------|
| `playwright_get` | GET 请求 | 获取 API 数据 |
| `playwright_post` | POST 请求 | 提交数据 |
| `playwright_put` | PUT 请求 | 更新数据 |
| `playwright_patch` | PATCH 请求 | 部分更新 |
| `playwright_delete` | DELETE 请求 | 删除数据 |

### 测试生成

| 工具 | 功能 | 示例 |
|------|------|------|
| `start_codegen_session` | 开始录制 | 记录操作 |
| `end_codegen_session` | 结束录制 | 生成测试代码 |
| `get_codegen_session` | 获取会话信息 | 查看录制状态 |
| `clear_codegen_session` | 清除会话 | 取消录制 |

### 响应等待

| 工具 | 功能 | 示例 |
|------|------|------|
| `playwright_expect_response` | 等待响应 | 等待 API 调用 |
| `playwright_assert_response` | 验证响应 | 断言响应内容 |

---

## 基础使用

### 场景 1：打开网页并截图

**用户请求**：
```
请打开 https://example.com 并截图
```

**Claude 执行**：
1. 调用 `playwright_navigate` 打开网页
2. 调用 `playwright_screenshot` 截图
3. 返回截图给用户

### 场景 2：填写表单并提交

**用户请求**：
```
请在 https://example.com/login 登录，用户名 test@example.com，密码 password123
```

**Claude 执行**：
1. 导航到登录页面
2. 填充用户名输入框
3. 填充密码输入框
4. 点击登录按钮
5. 截图确认登录成功

### 场景 3：提取页面内容

**用户请求**：
```
请访问 https://news.ycombinator.com 并提取前 10 条新闻标题
```

**Claude 执行**：
1. 导航到页面
2. 调用 `playwright_get_visible_text` 获取文本
3. 解析并返回新闻标题列表

---

## 与 CCG 集成

### 集成模式

Playwright MCP Server 可以与 CCG 协作体系深度集成：

| 角色 | 职责 | 使用场景 |
|------|------|---------|
| **Claude** | 直接调用 Playwright 工具 | 快速测试、页面分析、截图 |
| **Coder** | 生成 Playwright 测试代码 | 编写完整的 E2E 测试套件 |
| **Codex** | 审核测试代码质量 | 确保测试覆盖率和可维护性 |
| **Gemini** | 前端测试专家 | 复杂 UI 交互测试 |

### 模式 1：Claude 直接使用（快速测试）

**适用场景**：
- 临时测试某个功能
- 快速验证 UI 行为
- 页面截图和分析
- 调试前端问题

**示例**：
```
用户：请测试登录功能是否正常
Claude：
1. 打开登录页面
2. 填写测试账号
3. 点击登录
4. 验证是否跳转到首页
5. 截图确认
```

### 模式 2：Coder 生成测试代码（持久化测试）

**适用场景**：
- 需要可重复执行的测试
- 集成到 CI/CD 流程
- 完整的测试套件
- 回归测试

**工作流**：
```
1. 用户提出需求："为登录功能生成 E2E 测试"
2. Claude 调用 Coder 生成测试代码
3. Coder 使用 Playwright API 编写测试
4. Claude 验收测试代码
5. Codex 审核测试质量
```

**Coder Prompt 模板**：
```
请使用 Playwright 为以下功能生成 E2E 测试：

**功能描述**：用户登录
**测试场景**：
1. 正常登录流程
2. 错误密码处理
3. 空字段验证

**技术要求**：
- 使用 Playwright Test 框架
- 包含 Page Object Model
- 添加适当的等待和断言
- 包含错误处理

**验收标准**：
- 所有测试用例通过
- 代码可读性良好
- 包含必要的注释
```

### 模式 3：Gemini 处理复杂 UI 测试

**适用场景**：
- 复杂的前端交互（拖拽、动画）
- 需要深入理解 UI 框架（React、Vue）
- 性能测试和优化
- 跨浏览器兼容性测试

**触发条件**：
- 用户明确要求使用 Gemini
- 涉及复杂前端技术栈
- Claude 判断需要前端专家视角

### 完整工作流示例

**需求**：为电商网站的购物车功能生成 E2E 测试

**步骤 1：Claude 分析需求**
```
1. 识别测试场景（添加商品、修改数量、删除商品、结算）
2. 确定技术栈（React + TypeScript）
3. 决定使用 Coder 生成测试代码
```

**步骤 2：调用 Coder 生成测试**
```
Claude 调用 mcp__ccg__coder，传递详细的测试需求 Prompt
```

**步骤 3：Claude 验收**
```
1. 检查测试文件结构
2. 验证测试场景覆盖
3. 确认代码风格符合规范
```

**步骤 4：Codex 审核**
```
Claude 调用 mcp__ccg__codex，Codex 审核：
1. 测试覆盖率是否充分
2. 断言是否合理
3. 是否有潜在的 flaky tests
4. 是否遵循最佳实践
```

**步骤 5：执行测试**
```
Claude 运行测试，确认全部通过
```

---

## 最佳实践

### 1. 选择器策略

**优先级**（从高到低）：
1. `data-testid` 属性（最稳定）
2. `aria-label` 或 `role` 属性（语义化）
3. 文本内容（适合按钮、链接）
4. CSS 类名（避免使用，容易变化）
5. XPath（最后选择）

**示例**：
```javascript
// ✅ 推荐：使用 data-testid
await page.click('[data-testid="login-button"]');

// ✅ 推荐：使用 aria-label
await page.click('[aria-label="Submit form"]');

// ⚠️ 可接受：使用文本
await page.click('text=Login');

// ❌ 不推荐：使用 CSS 类名
await page.click('.btn-primary.login-btn');
```

### 2. 等待策略

**自动等待**：
- Playwright 自动等待元素可见、可点击
- 大多数情况无需手动等待

**显式等待**（特殊情况）：
```javascript
// 等待网络请求完成
await page.waitForResponse(resp => resp.url().includes('/api/data'));

// 等待元素状态
await page.waitForSelector('[data-testid="result"]', { state: 'visible' });

// 等待导航
await page.waitForNavigation();
```

### 3. 测试隔离

**每个测试独立**：
```javascript
test.beforeEach(async ({ page }) => {
  // 每个测试前重置状态
  await page.goto('https://example.com');
  await page.evaluate(() => localStorage.clear());
});
```

### 4. 截图和调试

**失败时自动截图**：
```javascript
test('login test', async ({ page }) => {
  try {
    await page.goto('https://example.com/login');
    await page.fill('[name="email"]', 'test@example.com');
    await page.click('[type="submit"]');
  } catch (error) {
    await page.screenshot({ path: 'failure.png' });
    throw error;
  }
});
```

### 5. Page Object Model

**封装页面逻辑**：
```javascript
// pages/LoginPage.js
class LoginPage {
  constructor(page) {
    this.page = page;
    this.emailInput = '[name="email"]';
    this.passwordInput = '[name="password"]';
    this.submitButton = '[type="submit"]';
  }

  async login(email, password) {
    await this.page.fill(this.emailInput, email);
    await this.page.fill(this.passwordInput, password);
    await this.page.click(this.submitButton);
  }
}
```

---

## 常见问题

### Q1: Playwright MCP Server 和直接写 Playwright 代码有什么区别？

**A**:
- **MCP Server**：Claude 直接调用工具，无需编写代码，适合快速测试和探索
- **Playwright 代码**：可重复执行，集成到 CI/CD，适合持久化测试

**建议**：两者结合使用
- 用 MCP Server 快速验证功能
- 用 Coder 生成持久化测试代码

### Q2: 如何处理动态内容和异步加载？

**A**: Playwright 自动等待元素可见和可交互，大多数情况无需手动处理。特殊情况使用：
```javascript
// 等待特定响应
await page.waitForResponse(resp => resp.url().includes('/api/data'));

// 等待元素出现
await page.waitForSelector('[data-testid="result"]');
```

### Q3: 测试在本地通过但 CI 失败怎么办？

**A**: 常见原因和解决方案：
1. **时区差异**：使用 UTC 时间或固定时区
2. **屏幕尺寸**：显式设置视口大小
3. **网络速度**：增加超时时间
4. **并发问题**：确保测试隔离

### Q4: 如何测试需要登录的页面？

**A**: 使用 Playwright 的 Storage State 功能：
```javascript
// 1. 登录一次并保存状态
await page.goto('https://example.com/login');
await page.fill('[name="email"]', 'test@example.com');
await page.fill('[name="password"]', 'password');
await page.click('[type="submit"]');
await page.context().storageState({ path: 'auth.json' });

// 2. 后续测试直接使用保存的状态
const context = await browser.newContext({ storageState: 'auth.json' });
```

### Q5: 如何调试 Playwright 测试？

**A**: 多种调试方法：
1. **截图**：`await page.screenshot({ path: 'debug.png' })`
2. **控制台日志**：使用 `playwright_console_logs` 工具
3. **慢动作模式**：`{ slowMo: 1000 }` 参数
4. **Playwright Inspector**：`PWDEBUG=1` 环境变量

### Q6: Playwright 支持哪些浏览器？

**A**: 支持三大浏览器引擎：
- **Chromium**（Chrome、Edge）
- **Firefox**
- **WebKit**（Safari）

通过 `browserType` 参数指定：
```javascript
await page.goto('https://example.com', { browserType: 'firefox' });
```

---

## 总结

Playwright MCP Server 为 CCG 协作体系提供了强大的 Web 自动化能力：

**核心价值**：
- ✅ Claude 可以直接操作浏览器，无需编写代码
- ✅ Coder 可以生成持久化的 E2E 测试
- ✅ Codex 可以审核测试质量
- ✅ Gemini 可以处理复杂的前端测试

**使用建议**：
1. 快速验证：Claude 直接使用 Playwright 工具
2. 持久化测试：Coder 生成测试代码
3. 质量保障：Codex 审核测试覆盖率
4. 复杂场景：Gemini 处理前端专项测试

**下一步**：
- 查看 `/ccg-e2e-test` Skill 了解完整的测试生成流程
- 参考 `cases/playwright/` 目录中的实际案例
- 阅读 `ai/testing_strategy.md` 了解测试策略

**立即开始**：尝试让 Claude 打开一个网页并截图！
